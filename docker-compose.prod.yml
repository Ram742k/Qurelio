version: '3.8'

services:
  app:
    environment:
      APP_ENV: production
      APP_DEBUG: "false"

  web:
    ports:
      - "80:80"
      - "443:443"
    restart: always

  mysql:
    restart: always

  redis:
    restart: always

  worker:
    restart: always
